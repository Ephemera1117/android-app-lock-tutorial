# 更新日志

> 每次改动都记一笔：改了什么、为什么改、如果你已经照旧版实现了要注意什么。
> 只想知道最新一次改了什么，看最上面那节就行。

---

## 2026-09-19 —— 加了一把指纹

### 新增：3.1 节「再加一把指纹（生物识别）」

`03-lock-screen-ui.md` 里多了一整节，讲怎么在锁屏上加指纹解锁。这一章原来叫「PIN 锁屏界面」，现在叫「锁屏与验证方式」。

**为什么做**：6 位密码每天输十几遍太烦。指纹的价值不是更安全，是**不用输**。

**做法**：指纹挂在锁屏上已有的元素上（我们是顶部那个图标），点一下弹系统指纹框。锁屏的样子一点不用改，也不多一个"指纹"按钮。指纹和密码并存。

**这一节里最重要的是三条"先知道"**：

1. **指纹界面不能用你自己画的**。系统不允许第三方 App 自绘指纹界面（防钓鱼），你只能控制"什么时候弹"和"框上写什么字"。想做"图标本身就是扫描器"——做不到，别花时间。
2. **Activity 基类必须是 `FragmentActivity`**。不满足时**不报错不崩溃，只是点什么都没反应**。
3. **弹框的取消按钮文字是条件必填的**。不包含"设备密码"验证方式时必须给，否则直接崩。

也一并写了：怎么判断设备能不能用（`STRONG` 和 `WEAK` 都要试）、怎么从 Context 往上找 Activity、失败怎么计数、要不要给开关。

### 新增：两条踩坑记录（#16 #17）

`05-pitfalls.md` 的表格从 15 条加到 17 条：

- **#16** Activity 不是 `FragmentActivity` → 指纹点了没反应（**不报错的那种失败**，最难查）
- **#17** `PromptInfo.build()` 抛 `Negative text must be set and non-empty` → 直接崩

排查 #16 的姿势也记了：这台设备上 `Log.d` 根本读不到，**把中间结果落成文件再读**比等日志快得多。

### 新增：设置结构的坑（#18）和一条设计原则

- 表格加了一条 **#18**：给序列化的设置**加字段是安全的，改类型不是**（老 JSON 会解析失败）。加指纹时正好踩在这个边界上，顺手记下来。
- 「设计原则总结」加了第 6 条：**系统不让你做的事，别硬做**。指纹不能自绘、FLAG_SECURE 下拍不到自己的保护页、某些 ROM 不认 TaskDescription——先去确认"这件事到底能不能做"，再决定怎么实现。

### README 改了哪里

- 「做出来是什么效果」加了「指纹解锁（可选）」一节
- **「已知问题」加了两条指纹相关的局限**：「**指纹框的样子改不了**」（系统不允许自绘，能控制的只有什么时候弹、框上写什么字）和「**指纹还需要设备端配合**」（没录指纹/没传感器时要静默降级，不能让整把锁跟着失效）
- 「哪些直接能搬」表格加了指纹那行（通用程度标⭐⭐⭐，但注明**坑最集中**）
- 「可以改进的方向」里原来写着"验证方式可以换指纹"——现在真做了，改成指向 3.1 节
- 目录表更新，并加了本文件的入口

### 如果你已经照旧版实现完了

**不用改任何已有代码。** 这次全是增量：

- 不想要指纹 → 跳过 3.1 节，其他章节一字未动
- 想要指纹 → 直接照 3.1 节做。唯一要动已有代码的地方是 **Activity 基类改成 `FragmentActivity`**，这个改动本身不影响其他功能（`FragmentActivity` 是 `ComponentActivity` 的子类）

---

## 2026-09-17 —— 补充实践经验

- 补了 `signalLockScreenDrawn` 机制，以及 `lockEnabled` 关着时保护页永远不揭的那个 bug
- 修了「保护页卡住」：改用 `onWindowFocusChanged(true)` 揭 coverView，不再依赖 Compose 的 `SideEffect`（它不会在回前台时重新执行）
- 加了免责声明：不同设备/ROM/版本下可能有我们没遇到过的问题

## 2026-09-16 —— 首版

第一版发出来，包含：架构总览、锁屏状态机、PIN 锁屏界面、切后台隐私保护（coverView 方案）、踩坑总结、可拓展的方向。

当天陆续打磨了几轮：把「必须做对的」和「花活儿」拆开、语气对齐、去掉示例里的个人信息、加"我们实际做出来是什么样"、加适用环境和局限说明。
